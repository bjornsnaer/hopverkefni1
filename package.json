{
  "name": "hopverkefni1",
  "version": "0.0.1",
  "description": "verkefni",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "node-sass --source-map-embed styles.scss styles.css",
    "sass-watch": "node-sass --source-map-embed --watch styles.scss styles.css",
    "browser-sync": "browser-sync start --server --watch --file index.html styles.scss",
    "dev": "npm-run-all --parallel sass-watch browser-sync"
  },
  "author": "bjorn og gudlaug",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "node-sass": "^7.0.0",
    "npm-run-all": "^4.1.5"
  }
}
